---
/*======== LAYOUT ========*/
import PageLayout from '../layouts/PageLayout.astro';
/*======== TYPOGRAPHY ========*/
import Paragraph from '../components/typography/Paragraph.astro';
/*======== COMPONENTS ========*/
import ThoughtsTab from '../components/containers/ThoughtsTab.astro';
/*======== ASSETS ========*/
const allContent = Object.values(import.meta.glob('./thoughts/*.md', { eager: true }));
---

<PageLayout siteTabText="thoughts" pageTitle="thoughts." pageSubtitle="ブログ">
    
    <Paragraph>
        <b>tl;dr: </b> <i>food recipes, shower thoughts, and everything in between...</i>
    </Paragraph>

    <div class="flex-col">
        {
        allContent.map((post:any)=>(
            <ThoughtsTab url={post.url} frontmatter={post.frontmatter}/>
        ))}
    </div>    

</PageLayout>

